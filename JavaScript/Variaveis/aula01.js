// O famoso ; não e necessario no Javascript porem vamos manter o padrao pois, creio que no typescript a ultilização dele retorna

const nome = "Anderson" // Const e não pode ser alterado
let idade = 20 // pode ser alterado funcionando semilar ao var
let mais_idade = 2


console.log("Hello world!") // Faz a impressão na tela do terminal
console.log(nome + " " + (idade + mais_idade)) 
